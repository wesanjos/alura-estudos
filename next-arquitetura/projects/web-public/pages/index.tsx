import { sum } from "@alura-estudo/utils/math/sum"

export default function HomeScreen() {
  return (
    <main>
      <h1>Home Screen</h1>
      <p>Importação de módulo local: @alura/utils/math {sum(2, 2)}</p>
    </main>
  );
}
